<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Asistencia</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
        <link rel="stylesheet" href="style.css">
        <!-- <script src="../todos/scripts/asistencia.js"></script> -->
</head>

<body>
    <div class="container-md mt-5">
        <select class="form-select" id="selectRegion" name="selectRegion" aria-label="Default select example">
            <option selected>Seleccionar planta</option>
            <% for (const [key, datos] of Object.entries(resultadoPlanta)) { %>
                <option value="<%=datos.planta %>">
                    <%= datos.planta %>
                </option>
                <% } %>
        </select>
        <div class="d-grid gap-2">
            <button id="btnBuscar" class="btn btn-outline-primary">Buscar</button>
        </div>
        <div class="container-md mt-5">
            <% if (resultadoGch !='' ) {%>
                <%- include('partials/_tablaAsistencia'); %>
            <% } %>
        </div>
    </div>
</body>

<script>
    const btnEnviar = document.getElementById('btnBuscar')
const select = document.getElementById('selectRegion')

select.addEventListener('change',
  function(){
    var selectedOption = this.options[select.selectedIndex];
    console.log(selectedOption.value + ': ' + selectedOption.text);
    console.log('Boton presionado', select);
    console.log('hola2', select.values);

    
    location.href ='/asistencia/' + select.value
})

function enviarAsistencia(nombre, Plantas, estatus){
    console.log('Hola este mensaje debe de contener el nombre' + nombre + Plantas + estatus);
    
}
</script>

</html>